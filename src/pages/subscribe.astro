---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Subscribe | The Princeton Journal">
  <main class="mx-auto max-w-4xl px-6 py-10">
    <p class="text-sm font-medium uppercase tracking-wide text-gray-500">
      Subscribe
    </p>

    <h1 class="mt-3 text-4xl font-bold tracking-tight">
      Subscribe to The Princeton Journal
    </h1>

    <div class="mt-8 space-y-6 text-lg leading-8 text-gray-800">
      <p>
        Subscribe to receive email updates from The Princeton Journal. By subscribing, you agree to receive news and update emails. You can unsubscribe at any time.
      </p>

      <form
        name="newsletter-subscribe"
        method="POST"
        action="/subscribe-thank-you"
        data-netlify="true"
        netlify-honeypot="bot-field"
        class="mt-8 space-y-6 rounded-2xl border border-gray-200 p-6 shadow-sm"
      >
        <input type="hidden" name="form-name" value="newsletter-subscribe" />

        <p class="hidden">
          <label>
            Don’t fill this out if you’re human:
            <input name="bot-field" />
          </label>
        </p>

        <div>
          <label class="mb-2 block text-sm font-medium text-gray-700">
            Your Email
          </label>
          <input
            type="email"
            name="email"
            required
            placeholder="you@example.com"
            class="w-full rounded-xl border border-gray-300 px-4 py-3 text-base outline-none focus:border-black"
          />
        </div>

        <button
          type="submit"
          class="rounded-xl border border-black px-5 py-3 text-sm font-medium transition hover:bg-black hover:text-white"
        >
          Subscribe
        </button>
      </form>

      <p class="text-base text-gray-600">
        We use this list to send occasional news updates and major reporting from The Princeton Journal.
      </p>
    </div>
  </main>
</Layout>
